{
    "name": "purduecircle",
    "version": "0.1.0",
    "description": "Social Network to connect Purdue students. Built for the CS307",
    "private": true,
    "scripts": {
        "start": "cd backend && npm install && node app.js",
        "heroku-postbuild": "cd frontend && npm install && npm run build",
        "full_install": "cd backend && npm install & cd frontend && npm install",
        "backend": "cd backend && npm start",
        "frontend": "cd frontend && npm start",
        "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\""
    },
    "dependencies": {
        "concurrently": "^7.0.0"
    },
    "engines":{
        "node":"14.16.0"
    }
}
